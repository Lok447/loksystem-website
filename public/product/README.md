# Product Screenshots

Place public product screenshots in this directory.

Recommended primary screenshot path:

```text
public/product/loksystem-app-screenshot.png
```

The homepage automatically checks this path. When the image exists, the hero uses the real screenshot. When it is missing, the hero falls back to a high-fidelity product preview component.
